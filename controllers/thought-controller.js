const { User, Thought } = require('../models');

const thoughtController = {
    getThoughts{},
    getThoughtById{},
    createThought{},
    updateThought{},
    deleteThought{},
    addReaction{},
    removeReaction{},
}